name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  NEXT_PUBLIC_FIREBASE_API_KEY: AIzaSyDVfw75B2YxC6LnhxX609O6fqfX82TDy80
  NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ship-any.firebaseapp.com
  NEXT_PUBLIC_FIREBASE_PROJECT_ID: ship-any
  NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ship-any.firebasestorage.app
  NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: "126284027592"
  NEXT_PUBLIC_FIREBASE_APP_ID: 1:126284027592:web:eb6f820b0d19b68437f7fd
  NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID: G-ERM3T6SM8L
  GROQ_API_KEY: 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
  E2B_API_KEY: e2b_57e22eae22c2e91a9cd4cf7fe4db5b87bec23892

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Lint
        run: npm run lint
        continue-on-error: true

      - name: Type check
        run: npx tsc --noEmit
        continue-on-error: true

      - name: Build
        run: npm run build

  test:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Build
        run: npm run build

      - name: Run Playwright tests
        run: npm run test

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
