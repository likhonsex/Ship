# 🚀 শুরু করার গাইড

[English](../../GETTING_STARTED.md) · **বাংলা**

---

এই গাইড আপনাকে Ship সেটআপ এবং চালু করতে সাহায্য করবে।

## 📋 সূচিপত্র

- [প্রয়োজনীয়তা](#প্রয়োজনীয়তা)
- [ইনস্টলেশন](#ইনস্টলেশন)
- [কনফিগারেশন](#কনফিগারেশন)
- [প্রথম রান](#প্রথম-রান)
- [ব্যবহার](#ব্যবহার)
- [সমস্যা সমাধান](#সমস্যা-সমাধান)

---

## প্রয়োজনীয়তা

### সফটওয়্যার

| সফটওয়্যার | ভার্সন | ডাউনলোড |
|-----------|--------|----------|
| Node.js | 18+ | [nodejs.org](https://nodejs.org) |
| npm | 9+ | Node.js এর সাথে আসে |
| Git | 2.0+ | [git-scm.com](https://git-scm.com) |

### API কী (যেকোনো একটি)

- [OpenAI API Key](https://platform.openai.com/api-keys)
- [Anthropic API Key](https://console.anthropic.com/)
- [Groq API Key](https://console.groq.com/)

---

## ইনস্টলেশন

### ধাপ ১: রিপোজিটরি ক্লোন

```bash
git clone https://github.com/likhonsex/Ship.git
cd Ship
```

### ধাপ ২: ডিপেন্ডেন্সি ইনস্টল

```bash
# npm দিয়ে
npm install

# অথবা pnpm দিয়ে
pnpm install
```

### ধাপ ৩: ভেরিফাই করুন

```bash
npm --version
node --version
```

---

## কনফিগারেশন

### এনভায়রনমেন্ট ফাইল তৈরি

```bash
cp .env.example .env
```

### .env এডিট করুন

```env
# ═══════════════════════════════════════
# LLM প্রোভাইডার (যেকোনো একটি প্রয়োজন)
# ═══════════════════════════════════════

# OpenAI
OPENAI_API_KEY=sk-...

# অথবা Anthropic (Claude)
ANTHROPIC_API_KEY=sk-ant-...

# অথবা Groq
GROQ_API_KEY=gsk_...

# ═══════════════════════════════════════
# GitHub
# ═══════════════════════════════════════
GITHUB_TOKEN=ghp_...

# ═══════════════════════════════════════
# ঐচ্ছিক সেটিংস
# ═══════════════════════════════════════
LOG_LEVEL=info
MAX_TOKENS=4096
```

### GitHub Token তৈরি

1. GitHub → Settings → Developer settings → Personal access tokens
2. "Generate new token (classic)" ক্লিক করুন
3. প্রয়োজনীয় স্কোপ নির্বাচন করুন:
   - `repo` (সম্পূর্ণ)
   - `workflow`
   - `read:org`

---

## প্রথম রান

### ডেভেলপমেন্ট মোড

```bash
npm run dev
```

### প্রোডাকশন বিল্ড

```bash
npm run build
npm start
```

### সফল হলে দেখবেন

```
✓ Ship Agent চালু হয়েছে
✓ GitHub সংযুক্ত
✓ LLM প্রোভাইডার: OpenAI
✓ পোর্ট: 3000
```

---

## ব্যবহার

### ১. Issue তৈরি করুন

আপনার রিপোজিটরিতে একটি Issue তৈরি করুন:

```markdown
## কাজ
লগইন পেজে "পাসওয়ার্ড ভুলে গেছেন" লিংক যোগ করুন।

## বিবরণ
- লগইন ফর্মের নিচে লিংক থাকবে
- /forgot-password পেজে রিডাইরেক্ট করবে
```

### ২. Ship লেবেল যোগ করুন

Issue-তে `ship` লেবেল যোগ করুন অথবা Ship-কে অ্যাসাইন করুন।

### ৩. Ship কাজ শুরু করবে

Ship স্বয়ংক্রিয়ভাবে:
1. Issue বিশ্লেষণ করবে
2. কোড লিখবে
3. PR তৈরি করবে

### ৪. রিভিউ এবং মার্জ

PR রিভিউ করুন এবং প্রয়োজনে ফিডব্যাক দিন।

---

## সমস্যা সমাধান

### সাধারণ সমস্যা

<details>
<summary><strong>❌ "API Key Invalid" ত্রুটি</strong></summary>

**সমাধান:**
1. `.env` ফাইলে API কী সঠিক কিনা চেক করুন
2. কী-তে কোনো অতিরিক্ত স্পেস নেই নিশ্চিত করুন
3. কী এক্সপায়ার হয়নি নিশ্চিত করুন

```bash
# টেস্ট করুন
curl -H "Authorization: Bearer $OPENAI_API_KEY" \
  https://api.openai.com/v1/models
```
</details>

<details>
<summary><strong>❌ "GitHub Token Unauthorized" ত্রুটি</strong></summary>

**সমাধান:**
1. Token-এ সঠিক স্কোপ আছে নিশ্চিত করুন
2. Token এক্সপায়ার হয়নি চেক করুন
3. নতুন Token তৈরি করুন
</details>

<details>
<summary><strong>❌ Ship Issue-এ সাড়া দিচ্ছে না</strong></summary>

**চেকলিস্ট:**
- [ ] GitHub Actions সক্রিয় আছে?
- [ ] সঠিক লেবেল যোগ করা হয়েছে?
- [ ] Workflow permissions সঠিক?
- [ ] Secrets সেট করা আছে?
</details>

---

## পরবর্তী ধাপ

- 📖 [AGENTS.md](./AGENTS.md) - এজেন্ট বিস্তারিত
- 🤝 [CONTRIBUTING.md](./CONTRIBUTING.md) - অবদান গাইড
- 🏠 [README.md](./README.md) - প্রধান ডকুমেন্টেশন

---

## সাহায্য প্রয়োজন?

- 💬 [GitHub Discussions](https://github.com/likhonsex/Ship/discussions)
- 🐛 [Issue রিপোর্ট](https://github.com/likhonsex/Ship/issues/new)

---

<div align="center">

**শুভ কোডিং! 🚀**

</div>